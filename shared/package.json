{
  "name": "@nutrivoice/shared",
  "version": "1.0.0",
  "description": "Shared TypeScript types for NutriVoice AI Diet Planner",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
